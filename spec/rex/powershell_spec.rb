require 'spec_helper'

describe Rex::Powershell do
  it 'has a version number' do
    expect(Rex::Powershell::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
